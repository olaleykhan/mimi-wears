import { SHOP_DATA } from "../../../assets/directoryData.js";

const INITIAL_STATE = {
    shopData: SHOP_DATA
};



export const shopReducer = (state = INITIAL_STATE, action) => {
    return state;
};
